# AI书桌 Plugin 固定验收候选 183310

版本 `0.3.2+codex.20260924183310`。拟仅交付 `aideskx/aidesk` 的固定测试分支 `codex/platform-candidate-183310`，当前只有本地候选，未获本次公开确认、未推送、未提升 main／稳定源。`release.json` 沿既有合同使用 stable 字段及拟用 UTC 时间，不表示发布已经发生。稳定发布资格仍待必要验收。

## 准确工件与改动

冻结源码 `5b83e0d5a6022c00f2c373c52432c3510021611d`，46件包、758772字节，发行摘要 `cfac65d8d811f71b4310834eae0ebdaf86f2ea8ce411ffcbb18be8f916b6a00b`。本次逐件核对冻结包与本机开发来源的已安装包，字节及Git可执行位一致；不生成新cachebuster，不重建原生工件。

相对公开固定100459只改manifest版本、MCP版本头和原 `goal-service-contract.mjs`。消费者增加对正式平台年度权益回执的精确元组支持，保留原local回执、active状态、时间、原请求号及摘要验证；Skill、Hook、工具目录、连接地址及其他43件保持。marketplace标识、安装和认证策略原样保留。

候选在已公开固定100459的准确提交 `1fdba135bfbd196d08349ed23b6d9ed66f094773` 上建立；该来源已独立按远端完整SHA取回核同。本次只复用其已核本地bundle，不声称重新查询了远端。其包摘要为 `df0302033123618ddfeb68a6c09d342766a7bf48fdc46a7348437d911336f5a8`。

## 配套与已验证范围

配套开发服务 `aidesk-authority-api-dev-128`，源码 `46d9e1a8a5666c125af0fc0560aba53d1c03e539`，前向SQL `202609250002`。既有终态已核128唯一正流量100%、完整27项配置及其他控制保持、健康检查200。当前183310实际MCP连接与原账号接续已有证据。

既有一次获准真实499元年费验收已核：正常官网支付与唯一发权、183310官方CLI技术消费者受理正式年度权益并读回同原号回执、同订单原路全额退款成功、权益expired后拒绝新请求并保留完整旧回执。原配置及测试前连接已恢复，新单重新关闭。这是限定真实技术消费与结算证据，不是桌面自然入口或完整目标执行证明；付款钱包实际到账未独立观察。

旧100459与183310的同号恢复增量实验已覆盖正式回执完成、升级拒绝并not_found时保持unknown两支；0网络／0安装，临时恢复根已清理。已有回归结果按受测版本复用，本次不重跑全量测试。

## 待验与使用边界

183310普通新账号首登、新可见自然入口及目标／材料／回报／再次进入、公开固定ref安装、同候选MacBook和Windows关键路径、稳定自动更新仍须按各自真实结果验收；旧版已有账号入口、合成预登记、CLI及离线回归不代替这些结果。普通注册已有可信OTP来源链，当前保留实际路径待验，不把它写成已知源码缺陷。真实家庭与长期运营准入另按获准范围处理。

固定候选须在准确公开批准并按同一提交读回后，沿官方marketplace固定完整SHA安装和验收；本次没有安装、更新、登录或新建任务。固定ref的更新保护继续生效，不能把ahead或blocked当作稳定源升级成功。稳定更新须按既有流程完成受影响的混合版本、迁移恢复及同候选双机预验，再取得覆盖准确工件和传播影响的公开批准。

## 恢复与完整性

本机恢复点为原100459准确包及原开发来源；服务恢复点为127使用原126镜像和完整27项配置，保留SQL250002及全部已产生业务、支付、退款和删除事实。旧服务恢复后不能新增正式平台年度受理；出现outcome_unknown应沿原号对账，不生成新号重放。恢复由原所有者在明确终态后执行，不清认证、不降级数据库、不删业务记录。

最后已核官方main为 `bb6a7a9aceb33588ffa1fd254343608f336e486c`，版本 `0.3.2+codex.20260919141235`，摘要 `47d770ebe1967bb01f2db17eaa6081a345587c82933ce752aafb6cc5b8beaff3`。该历史稳定点只用于来源和恢复辨识，不授权自动降级；公开回退也须准确确认。发布前须fresh核远端refs及同版本无异包，本次本地准备不替代该检查。

本包、release.json、原marketplace、本文及SHA256SUMS同提交交付，校验和不包含自身。最低版本 `0.3.0` 是既有更新阈值，不代表全部旧版业务已验。包内既有原生provenance会公开私有仓名 `aideskx/aidesk-src`、CI链接及runner路径，原公开100459已有相同内容；不包含凭据。包内容发生变化必须另行版本化，不能覆盖同版本不同内容。
