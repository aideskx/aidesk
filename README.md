# AI书桌 Plugin 固定开发候选 100459

本候选版本为 `0.3.2+codex.20260924100459`，拟交付 `aideskx/aidesk` 的新分支 `codex/platform-candidate-100459`。这是待用户逐次确认的本地候选，尚未公开推送；不提升 main／稳定源，不表示商业开放或 Plugin Directory 上架。`release.json` 沿原发行合同保留 stable 字段，`publishedAt` 为拟用 UTC 时间，均不证明发布已经发生。

## 准确来源与改动

包冻结源为 `edfce2ddbdc59b51697a01d0930cee3e2390819b`。46个文件共758,566字节与当前本机已安装100459逐件核同，正式发行摘要为 `df0302033123618ddfeb68a6c09d342766a7bf48fdc46a7348437d911336f5a8`。包内字节与Git可执行位原样保留，没有重新生成cachebuster、修改Skill、重编译原生锁或改变连接地址。

本轮主线改动为：在原目标任务读取接口中提供本人最近目标及必要进展摘要，返回原任务与成果入口；仅向声明支持的新包暴露recent，旧包继续使用旧目录与snapshot／reports。旧安装来源与更新保护、账号、数据归属及原号恢复规则继续沿原所有者。现有三项举报工具保持；包内举报导出支持不代表服务开放，配套服务默认关闭该导出，不扩大到举报主线或商业政策。

本地候选从公开main基线 `bb6a7a9aceb33588ffa1fd254343608f336e486c` 单独建立，不包含旧232626候选的独立提交。marketplace `aidesk` 与 Plugin `aidesk-runtime`、安装及认证策略保持。

## 配套与实际验证

配套开发服务为 `aidesk-authority-api-dev` 的125版，源码同为 `edfce2ddbdc59b51697a01d0930cee3e2390819b`，前向SQL为 `202609240006_goal_task_recent`。Task2227307已完成，125为唯一正流量100%，完整20项环境配置和其他控制保持、健康检查200；SQL独立后检确认原12组目录指纹和8项计数保持。新125镜像URI与CCR摘要未在本批安全终态中单独取证，不能将旧124恢复摘要当作125摘要。

本机100459已沿原开发来源官方安装，配置、其他Plugin与marketplace保持。原书桌已实际采用100459，同一合成账号可读recent首页；另沿已知目标ID读回合成图目标v2、决定4、报告14及原任务和当前3313字节成果。首轮目标不在recent首页，因此这不证明分页找到指定目标。原生分页接续由总控另行验收，候选准备时仍在进行，本文不预写通过。磁盘安装、实际连接、元数据读取及UI进入分别认定。

原操作卡目标的已删响应已与此前用户删除指令及准确墓碑核同；不是125新增删除，也不恢复该目标。两个本机成果仍可读，不把独立文件存在当作服务目标已恢复。包升级不撤回删除或取消事实。

本机、合成资料及已有账号的有限通过，不代替普通新账号首登、公开固定ref安装、用户MacBook／Windows双机、稳定自动更新、完整混合版本、真实家庭或商业准入。既有两系统Node20／24的Hook与原生CI属于开发回归；100459复用同输入CI及原生工件，不宣称新增edfce CI或用户Windows真机验收。正式报价、收款及商业开放仍关闭。

## 固定候选使用与恢复

只有准确候选获准公开并独立读回后，才在安排好的预验环境沿官方入口固定批准的完整提交：

```sh
codex plugin marketplace add aideskx/aidesk --ref <经批准并独立读回的完整提交SHA>
```

按宿主正常安装和授权流程进入，新可见自然入口首句为“打开AI书桌”；已登录时按实际权限继续。固定ref或其他安装来源可能触发更新保护，不绕过、不用更新失败宣称最新版；此次准备不安装、登录或新建任务。

`minimumSupportedVersion=0.3.0` 沿用公开基线更新阈值，不构成全版本业务兼容保证。上一公开main `bb6a7a9aceb33588ffa1fd254343608f336e486c` 的包为 `0.3.2+codex.20260919141235`、摘要 `47d770ebe1967bb01f2db17eaa6081a345587c82933ce752aafb6cc5b8beaff3`；已公开固定232626分支保持。它们的存在不授权自动降级，公开回退也需逐次确认。

本机恢复保留准确060318的原包与原来源；服务恢复使用准确124镜像 `aidesk-authority-api-dev-124-20260924134747`，已核摘要 `sha256:0673251af482c9b4b84156e150af050b061194fda848cb841fd6f385fd4ac144`，保持完整20项配置、其他控制及全部前向SQL240005／240006和业务事实。恢复须由原发布所有者以新的准确窗口和意图执行，不复用已完成任务，不回退数据库、不清除墓碑。旧124不提供recent，新包须按实际工具目录回退读取；未知结果保留原号对账。

## 完整性与公开边界

包、release、原marketplace、本文和SHA256SUMS同提交交付；校验和不含自身。候选提交和恢复bundle仅在本地保存，提交成功不代表远端已经发布。后续须以准确提交、版本、包摘要和上述未验范围取得本次明确确认，再公开推送并独立读回。

原生工件的既有provenance含私有源码仓名 `aideskx/aidesk-src`、该仓CI链接及构建runner来源路径，会随原包公开；这些来源信息不是凭据。包内已有文字原样保留，不夹带私有业务资料或认证材料。若改变包内容，必须另建候选并相应验证，不能暗改本次已安装原包。
