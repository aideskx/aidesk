# AI书桌 Plugin 技术预发布

普通用户向 Codex 发送：

> 从GitHub安装Codex插件 aideskx/aidesk

安装后新建任务，发送“打开AI书桌”。官网：https://aideskx.com

当前版本 `0.3.2+codex.20260914075013`，同一插件包含入口 Skill、MCP 连接与一次性更新程序。账号与教学能力以实际授权和当前工具为准；教学功能尚未开放。

打开AI书桌会检查官方稳定版本，需要时通过宿主官方 Plugin CLI 安装。只想检查或暂不更新，可以明确告诉 Codex。不会关闭 Codex 后后台轮询；检查失败不会冒充最新版。GitHub 公开下载层与服务均有缓存，发布后各地区可能需数分钟才看到新版。发现新版、磁盘已安装、当前连接已加载是分别核验的状态；新任务仍使用旧连接时可能需要重启 Codex。

更新依赖宿主已有 Node（20+）、可核实的 Codex CLI 与执行能力，宿主必要确认仍然有效。来源冲突、固定其他版本或缺少执行能力时，会明确说明停点，并保留上方安装指令。现有不含自动更新程序的旧包需先沿此指令升级一次。此前本机已用首句仅“打开AI书桌”的新任务，完成到 `0.3.2+codex.20260914062319` 的自动安装、文件核验和再次使用；后续只读检查确认新连接携带该版本。此前受测任务由官方任务工具创建，受测过程无需重启；不据此承诺其他宿主、所有启动时序或跨设备均通过。本构建只调整版本标识，算法与 Skill 保持不变。

2026-09-14，本机再次通过官方任务工具创建的两个新任务验收，首句均只有“打开AI书桌”。第一个任务从 `0.3.2+codex.20260914062319` 自动安装到当前版本，安装程序正常退出、未触发修复；独立 CLI 读回确认当前版本已启用且来源正确，7 个实际包文件的摘要匹配。该安装任务的旧 MCP 连接没有热刷新。第二个任务已读取当前版本 Skill，完成真实 MCP 更新检查、账号与上下文读取，未再次运行安装。随后只读 MCP 检查于 `2026-09-14T07:58:52.670Z` 返回 `current`，连接携带版本与最新版本均为 `0.3.2+codex.20260914075013`。本轮没有重启；未测试用户手动新建任务、双机自动更新、教学或新登录，不将本次结果扩大到这些场景。

发布元数据 `release.json` 与完整包在同一 Git 提交中；更新程序从固定官方来源读取，安装后按 `packageDigest` 核验实际文件。当前摘要 `20478878bc5b9f584e50a740bfa8ef7eccabd89f1b0521af5e6acff94e1aadcd`，`SHA256SUMS` 覆盖全部7个包文件与发布元数据。上一可恢复公开提交 `e04379d1eb45330eb8521651c4ebf6e7724f8994`（`0.3.2+codex.20260914062319`）保留在历史中，恢复必须使用同提交配套包与元数据，不自动降级。

这是 PUBLIC 技术分发仓，不是私有源码仓，也不是 OpenAI Plugin Directory。
