# AI书桌 Plugin 技术预发布

这是 AI书桌 Plugin 的技术预发布分发仓，不是 AI书桌私有源码仓，也不是 OpenAI 公共 Plugin Directory。

当前仅验证 `aidesk-runtime` 0.1.0 最小入口；不承诺 Windows、自动更新、静默更新或完整产品能力。

普通用户可以直接向 Codex 发送以下一句话：

> 从 https://github.com/aiepgpt-glitch/aidesk-plugin 安装 AI书桌 Plugin，安装完成后新建任务并打开 AI书桌。

Codex 内部实际使用 `codex plugin marketplace add` 与 `codex plugin add` 完成安装。

本次技术预发布对应不可移动标签 `aidesk-runtime-v0.1.0`。

`packageDigest` 为 `801c7f0c8c1c15ea1104480e270bf7b106ca6e2c72000dc9040fdc5f23ee7b3e`，仅作为项目审计证据；不得据此声称 Codex 会自动验证该摘要。
