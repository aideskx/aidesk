# AI书桌 Plugin 固定开发候选

本文件记录 `0.3.2+codex.20260923232626` 的本地准备时点；尚未公开推送，也未从本公开候选安装或改变任何连接。拟用分支为 `codex/platform-candidate-232626`，须对准确提交取得本次公开确认后再交付并独立读回。固定分支交付不提升 main／stable，不等于商业上线、真实家庭开放或 OpenAI Plugin Directory 上架。`release.json` 的 `publishedAt` 是拟用 UTC 时间，不证明已发生发布；其 stable 字段沿用发行工具合同，不是本次已提升稳定源的证据。

包源为 `f705c2cb72c231d9c9e902c41afb3646784307a0`，45个文件共712,190字节及可执行位原样复用已验232626冻结包。正式发行摘要按排序后的 `{path,gitMode,sha256}` 计算，为 `7b92896feec851a89d8524a99469fb39670163cf668b950f2564ef803d253b69`。没有重新生成cachebuster、修改Skill或重编译原生锁；包内“本地开发候选”等文字亦原样保留。本候选从公开基线 `bb6a7a9aceb33588ffa1fd254343608f336e486c` 单独建立，不包含旧162740本地候选提交。

## 安装与使用

公开来源仍为 `aideskx/aidesk`，Plugin为 `aidesk-runtime`，marketplace为 `aidesk`。372字节的marketplace、安装与认证策略未改。

仅在准确候选获准公开并读回后，在无同名来源冲突的预验环境，用官方CLI固定已经确认的完整提交：

```sh
codex plugin marketplace add aideskx/aidesk --ref <经批准并独立读回的完整提交SHA>
```

随后沿宿主正常安装入口选择Plugin，核磁盘232626版本、包摘要、来源和实际MCP连接；加入marketplace或磁盘安装均不证明连接已加载。固定ref用于限定候选预验，现有更新器可能按固定／不同来源保护停止更新，不绕过该保护。普通默认来源不指定此ref，取得的是届时实际公开的默认版本。

新可见任务第一句只发送“打开AI书桌”。需要时经宿主支持的正常网页登录与授权继续；已有有效授权按服务当前结果使用。更新查询失败不能称为最新版，版本响应ahead也不证明更新安装成功。未知业务结果保持原号对账，not_found非终态不成为重新派发许可。官网为 https://aideskx.com；当前权限及服务状态仍须按实际读取核实。

## 配套与已验范围

本候选沿现有账号、目标与网络所有者提供个人目标、独立任务接续、必要材料／进展／结果、自愿分享／采用／回应及选定内容的数据操作。独立帖子删除覆盖本人该帖全部修订，独立回应只删除准确本人回应；合法采用副本及其他回应不随作者源帖删除而清除。单目标删除另沿原合同处理该目标与关联内容，不引入全账号擦除或新保留政策。

配套为开发服务 `aidesk-authority-api-dev` 的123版，部署源 `fbd91e9b2a538a6c6dec5b98e724216c4499c05d`，前向SQL `202609240002_goal_network_delete`。最近一次部署验收读回确认服务123为唯一100%，原19项配置与其他控制、health200保持；SQL独立前后检保护原结构／权限／数据。此本地候选准备没有再部署服务、应用SQL或变更认证。

实际验收只覆盖当前Mac、已有专用合成账号与记录的可见任务：

- 已验221552先完成同一C的旧冲突原号取消、P/R/Q/T独立删除及Post清理；这属于此前包的限定结果，不伪称当时已运行232626。
- 232626新可见任务从仅“打开AI书桌”进入，在已有C登录下完成唯一新帖U两修订：旧快照冲突后，同号not_found的真实Post直接给出已匹配本机恢复根，沿完整原件取消并释放意图，再以新快照删除。Post自动清理13文件／3原号、无警告；两publish薄映射、已删read的真实Pre拒绝以及所选旧业务投影保持。没有额外发布来取得恢复根。
- 同一232626任务在C/D串行正常OAuth下完成跨用户保护：C删新源帖后，D采用副本与独立回应正文／hash保持，仅来源为withdrawn；D删回应后采用和新goal仍保留，再独立清理本批新goal。三次Post清4／11／10文件且无警告；C旧投影保持，D旧投影比对在回应删除后、新goal删除前。源帖正常read不可得，但原读取号operation仍not_found／terminal:false，保留非终态，不能称所有原号都已终态。

最终真实读取确认恢复C authenticated／active；A/B身份工具当时不可用，本轮未验，未登录或改其连接。独立成功放行Pre未观察，不能由Post或后续原件追认。首轮更新unavailable／different_installation_source、goal清理中总控额外要求计数字段的停点及后续按实际版本／快照合同纠正均保留。

私有CI计数修复后的两平台×Node20／24每组115项通过，Windows仅跳过既有POSIX权限项；之前113计数门导致的失败仍保留。CI不等于用户MacBook／Windows宿主安装、连接或业务验收。普通首次登录、同时双端、两个平台付费新用户、完整混合版本、公开固定ref安装／更新、同候选用户双机和真人使用仍未通过。本候选不决定价格、旧权益映射、付款／退款或商业准入，正式交易与商业开放保持关闭。

## 兼容与恢复

`minimumSupportedVersion=0.3.0` 原样沿用公开基线的更新阈值，不增加全版本业务兼容保证。上一公开包恢复点仍为 `bb6a7a9aceb33588ffa1fd254343608f336e486c`，版本 `0.3.2+codex.20260919141235`、摘要 `47d770ebe1967bb01f2db17eaa6081a345587c82933ce752aafb6cc5b8beaff3`，其完整历史保留；这不证明该旧包与当前SQL／新数据相容，不应自动降级。

服务恢复由原发布所有者使用122所用的原120镜像，保持原19项配置和其他控制，保留前向240002、全部旧新数据与删除／取消薄事实。恢复服务或包不会撤回已确认删除、恢复已删源正文或取消既定终态；不得DOWN、清除墓碑或恢复不识别删除的旧SQL。原221552本机源备份另保留，任何恢复仍按准确组合和授权执行，公开回退也须另获本次确认。

本机清理仅覆盖准确归属的Plugin自管原件；证据副本、宿主历史、引用文件、独立导出、其他设备及供应方物理备份不在上述删除结论内。无法确认或未知的原件保持并如实对账。

## 本地候选完整性与后续交付

包、release、未改marketplace、本文与SHA256SUMS位于同一候选提交；同提交校验只证明工件完整性。本轮无公开写入、安装、连接刷新、云变更或业务重跑。实际发布前复核ref、版本碰撞、包摘要、配套和恢复条件，再取得针对本次准确候选的明确确认；公开后还须按同提交独立读回，并分别核固定ref安装／连接与安排中的设备路径。

原生锁provenance原样包含私有源码仓名称 `aideskx/aidesk-src`、该仓CI URL及GitHub runner路径；与旧162740本地候选相同，尚未公开，不含登录凭据。这些披露随本候选进入准确审批范围；若需更改包内信息，须另建版本及对应验证，不能暗改本已验232626包。
